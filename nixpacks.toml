[phases.setup]
nixPkgs = ["python3", "ffmpeg"]

[phases.install]
cmds = ["python -m pip install -r backend/requirements.txt", "playwright install chromium"]

[start]
cmd = "playwright install chromium && cd backend && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 2"
